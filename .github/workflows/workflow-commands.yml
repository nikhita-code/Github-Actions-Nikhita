name: "workflow-commands Job"

on:
  workflow_dispatch:   # allows you to run it manually from GitHub Actions UI

jobs:
  workflow-commands:
    runs-on: ubuntu-latest
    steps:
      - name: "group-logging"
        run: |
          echo "::group::my group msgs"
          echo "msg1"
          echo "msg2"
          echo "::endgroup::msgs ends"

